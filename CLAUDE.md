# 일정관리 시스템 (schedule-manager) — 프로젝트 메모리

## 무엇을 만드는가
목표 오픈일에서 **역산(백워드 스케줄링)**한 프로젝트 일정 관리 웹앱.
WBS·간트·M/M(맨먼스)·크리티컬 패스를 계산하고, 관리자(PM)가 태스크·설정을 편집한다.
현재 단일 HTML 프로토타입을 **React + 백엔드(API·DB·인증)** 3-tier로 이관 중.

## 설계 정본 (반드시 먼저 읽을 것)
- `docs/01_마이그레이션_설계서.md` — 아키텍처·스택·DB 스키마·API·RBAC·단계(P0~P4). **이 문서가 기준.**
- `reference/scheduler.reference.mjs` — 계산 엔진 참조 구현(순수 함수). 여기서 `packages/scheduler`(TS)로 이식.
- `reference/wbs_model.json` — 시드 데이터/모델 예시.
- `reference/wbs_calc.py` — 동일 로직의 파이썬본(정합성 참조).
- `reference/current_app.html` — 현재 동작하는 단일 파일 앱(화면·UX·편집모드 정본. 최신 UI 반영).

## 배경 문서 (요구사항·기획 맥락이 필요할 때)
- `docs/02_필요항목_기획서.md` — 데이터 항목(필드)·분류값 정의의 출처.
- `docs/03_화면기획서.md` — IA·화면 흐름·화면별 상세 기획.
- `docs/04_관리자_편집기능_계획.md` — 편집(수정·생성·설정) 기능·권한 계획.
- `docs/00_실행가이드.md` — 이 킷을 사람(운영자)이 쓰는 방법.

## 절대 규칙
- **계산 규칙을 바꾸지 말 것.** 엔진은 참조 구현과 동치여야 한다. 아래 골든값은 회귀 테스트로 고정한다.
- 계산 엔진은 **순수 함수**(I/O·전역 없음)로 유지하고 프론트·백엔드가 **공유**한다.
- 저장 요청은 서버에서: 검증(필수값·**순환의존성 차단**) → 트랜잭션 저장 → 엔진 재계산 → 감사로그.
- 권한은 프론트 숨김 + **백엔드 강제**(RBAC: admin/editor/viewer) 이중.

## 골든값 (회귀 테스트 필수 — 현재 시드 기준)
- 총 124 MD = 6.2 M/M, 완료 28 MD(23%)
- 주말 제외(off) 목표 오픈 = 2026-09-23 (ASAP 09-16 + 버퍼 5근무일)
- 주말 포함(on) 목표 오픈 = 2026-09-04 (ASAP 08-30)
- 크리티컬 패스 = 12개 태스크
- 3.3 공수 10→20 변경 시 off 목표 = 2026-10-06

## 스택 (설계서 3장)
모노레포(pnpm+Turborepo): `packages/scheduler`(TS 순수엔진) · `apps/web`(React+TS+Vite+Tailwind) ·
`apps/api`(NestJS+TS) · `prisma`(PostgreSQL). 백엔드도 TS로 엔진 공유.

## 작업 방식
- 단계(P0→P4)를 **하나씩** 진행하고 각 단계 끝에 커밋한다.
- 새 코드는 항상 테스트와 함께. P0의 골든 테스트를 먼저 통과시킨 뒤 다음 단계로.
- 큰 변경 전에는 계획을 먼저 제시하고 확인받는다.
